{
  "name": "shortened-url",
  "version": "1.0.0",
  "description": "Converts long URLs to short URLs, making it convenient for users to share URLs.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yongseok.dev",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
